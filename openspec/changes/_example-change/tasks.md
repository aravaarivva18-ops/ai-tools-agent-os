# Tasks: <название фичи>

<!--
Это чек-лист, который читает и loop.sh, и /goal (Claude обязан показать в диалоге, что
прогнал gates.sh и что все пункты отмечены — evaluator сам файлы не читает).
Берётся первый неотмеченный [ ] пункт, делается именно он, гейтится через
scripts/gates.sh, отмечается [x], коммитится, переход к следующему. Каждый пункт —
маленький и проверяемый (как в TDD).
-->

- [ ] 1.1 <конкретный маленький шаг, который можно реализовать и протестировать изолированно>
- [ ] 1.2 <следующий шаг>
- [ ] 2.1 <следующая логическая группа>
